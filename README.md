# csv_to_postgres
This Repository loads a csv file to a dataframe. Reads it and load the dataframe to a Postgres table

as a initial commit this rep aims the practive and training of python knowledge
